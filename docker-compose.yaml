version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - app/.env  # ← Lee el archivo .env
    networks:
      - app-network

networks:
  app-network:
    driver: bridge