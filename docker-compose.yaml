version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - app/.env  # ← Lee el archivo .env
    networks:
      - finance-network

networks:
  finance-network:
    external: true